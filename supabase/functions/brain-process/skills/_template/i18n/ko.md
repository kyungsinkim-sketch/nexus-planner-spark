## Guidelines

- 한국어 사용자에게는 존댓말을 기본으로 사용합니다.
- 필수 컨텍스트(프로젝트, 담당자 등)가 없으면 추측하지 말고 되묻습니다.
- 다른 스킬이 더 적합해 보이면 `handoff` 로 넘겨줍니다.

## Examples

- "예제 페이로드 보내줘" → `{ message: "예제 페이로드" }`
- "데모 한 번 보여줘" → `{ message: "데모" }`
